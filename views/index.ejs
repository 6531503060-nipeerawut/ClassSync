<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Home Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* General Styling */
        body {
            margin: 0;
            font-family: 'Poppins', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: url(https://img.freepik.com/free-photo/anime-style-galaxy-background_23-2151133978.jpg?t=st=1731834233~exp=1731837833~hmac=6297c6be2b10df2b8c9f60b13e27325f73da624a55b89d03ac57f97e61885b90&w=900) no-repeat center center/cover;
            color: #fff;
            position: relative;
            overflow: hidden;
        }

        /* Background Overlay */
        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            z-index: 1;
        }

        /* Content Styling */
        .container {
            position: relative;
            z-index: 2;
            padding-top: 5rem;
            text-align: center;
        }

        h1 {
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 2rem;
        }

        /* Cards Styling */
        .card {
            background: hsla(0, 0%, 42%, 0.378);
            border: 2px solid hsl(0, 0%, 100%);
            border-radius: 1rem;
            font-weight: bold;
            padding: 2rem;
            width: 90%;
            max-width: 600px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
        }

        .card:hover {
            transform: translateY(-10px);
        }

        .card-body {
            text-align: center;
        }

        .btn-primary {
            background-color: #ffffff;
            border: none;
            border-radius: 1rem;
            color: #000;
            letter-spacing: 1px;
            width: 100%;
            padding: 1rem;
            transition: 0.3s;
        }

        .btn-primary:hover {
            background-color: #ffffff;
            color: #320455;
            letter-spacing: 2px;
        }

        /* Responsive Layout */
        .row {
            margin-bottom: 2rem;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- Content -->
    <div class="container">
        <h1>Welcome to ClassSync</h1>

        <div class="row mt-5 justify-content-center">
            <div class="col-md-4">
                <div class="card">
                    <div class="card-header">
                        Student
                    </div>
                    <div class="card-body text-center">
                        <p class="card-text">Please login first to access your account.</p>
                        <a href="/login" class="btn btn-primary btn-lg">Student Login</a>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card">
                    <div class="card-header">
                        Instructor
                    </div>
                    <div class="card-body text-center">
                        <p class="card-text">Please login first to access your account.</p>
                        <a href="/login" class="btn btn-primary btn-lg">Instructor Login</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
