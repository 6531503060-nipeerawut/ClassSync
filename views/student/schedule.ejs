
<!DOCTYPE html> 
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Class Schedule</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css"> <!-- Assuming Bootstrap is set up -->
    <style>
        /* Optional: Add additional styles to match your design */
        .schedule-table th, .schedule-table td {
            text-align: center;
            vertical-align: middle;
        }
        .name-input, .class-input {
            width: 200px;
            margin-bottom: 10px;
        }
    </style>
    </head>
    <body class="bg-light">
        <div class="container">
            <h1 class="text-center my-4">Class Schedule</h1>
            
            <div class="d-flex justify-content-between mb-4">
                <div>
                    <label for="fullName" class="form-label">Full Name: <%= user.full_name %></label>
                    
                </div>
                
                <div>
                    <label for="schoolName" class="form-label">School: <%= user.student_school %></label>
       
                </div>
            </div>
    
            <table class="table table-bordered table-hover schedule-table">
                <thead class="table-light">
                    <tr>
                        <th>Time</th>
                        <% days.forEach(function(day) { %>
                            <th><%= day %></th>
                        <% }); %>
                    </tr>
                </thead>
                <tbody>
                    <% timeSlots.forEach(function(time) { %>
                        <tr>
                            <td><%= time %></td>
                            <% days.forEach(function(day) { %>
                                <td class="hover:bg-light" style="min-width: 120px; height: 50px;">
                                    <!-- Empty cell for time-slot -->
                                </td>
                            <% }); %>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    
        <script src="/js/bootstrap.min.js"></script> <!-- Assuming Bootstrap JS is set up -->
    </body>
    </html>