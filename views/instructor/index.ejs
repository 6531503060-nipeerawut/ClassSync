<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instructor Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Font */
        @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap");

        body {
            font-family: 'Poppins', sans-serif;
            background: url('../public/images/ajbg.jpg') ;
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
        }

        /* Navbar */
        .navbar {
            background-color: #ffffff00;
            border-bottom: 0.5px solid white;
        }
        .navbar a {
            text-decoration: none;
             color: white;
            padding: 10px 20px;
            display: inline-block;
        }

        /* Hover effect for menu items */
            .navbar a:hover {
                text-decoration: underline; 
                text-decoration-color: white;
        }
        .navbar-brand, .nav-link {
            color: #ffffff !important;
            font-weight: 500;
        }

        /* Tabs */
        .nav-tabs .nav-link.active {
            color: #ffffff ;
            font-weight: 600;
            background-color: #a0a3eb;
            border-color: #dee2e6 #dee2e6 #f5f8fb;
        }

        /* Card */
        .card {
            border: thin;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease-in-out;
            background-color: #fffdfd9e;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        /* Button */
        .btn-primary {
            background-color: #a0a3eb;
            border-radius: 20px;
            letter-spacing: 0.5px;
            transition: all 0.3s ease-in-out;
        }
        .btn-secondary{
            background-color: #e485cf;
            border-radius: 20px;
            letter-spacing: 0.5px;
            transition: all 0.3s ease-in-out;
        }
        .btn-secondary:hover {
            background-color: #ef35bd;
        }
        .btn-danger{
            background-color: #f3605d;
            border-radius: 20px;
            letter-spacing: 0.5px;
            transition: all 0.3s ease-in-out;
        }
       .btn-warning {
            background-color: #e485cf;
            border-radius: 20px;
            letter-spacing: 0.5px;
            transition: all 0.3s ease-in-out;
        }
        .btn-primary:hover {
            background-color: #afd1f7;
        }

        /* Table */
        .table {
            background: rgba(255, 255, 255, 0.6);
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .table th, .table td {
            padding: 1rem;
            vertical-align: middle;
        }

        /* Modal */
        .modal-content {
            border-radius: 10px;
        }
    </style>
</head>
<body>

    <!-- Navbar -->
     <nav class="navbar navbar-expand-lg navbar-light mb-3">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Instructor Portal</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/i/course">Class</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link "  href="/pf/profileI">Profile</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

   <!-- Tabs -->
   <div class="container mt-4">
    <ul class="nav nav-tabs" id="myTab" role="tablist">
        <li class="nav-item" role="presentation">
            <button class="nav-link active" id="home-tab" data-bs-toggle="tab" data-bs-target="#tab1" type="button" role="tab" aria-controls="tab1" aria-selected="true">Dashboard</button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="profile-tab" data-bs-toggle="tab" data-bs-target="#tab2" type="button" role="tab" aria-controls="tab2" aria-selected="false">Response</button>
        </li>
    </ul>
    <div class="tab-content mt-3" id="myTabContent">
        <!-- Tab 1: Current Page -->
        <div class="tab-pane fade show active" id="tab1" role="tabpanel" aria-labelledby="home-tab">
            <!-- Sidebar and Content -->
            <div class="container-fluid">
                <div class="row">
                    <!-- Main content -->
                    <main class="col-md-12 ms-sm-auto col-lg-12 px-md-4">
                        <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                            <h1 class="h2">Dashboard</h1>
                        </div>
                        <!-- Dashboard Cards -->
                        <div class="row">
                            <div class="col-md-4">
                                <!-- Card Triggering the Modal -->
                                <a class="card text-decoration-none" style="color:black;" href="#" data-bs-toggle="modal" data-bs-target="#createCourseModal">
                                    <div class="card-body">
                                        <h5 class="card-title">Create Class</h5>
                                    </div>
                                </a>
                            </div>

                            
                        <!-- Display Success or Error Message -->
                        <% if (message) { %>
                            <% if (errState) { %>
                                <div class="alert alert-danger alert-dismissible fade show my-3" role="alert">
                                    <%= message %>
                                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                </div>
                            <% } else { %>
                                <div class="alert alert-success alert-dismissible fade show my-3" role="alert">
                                    <%= message %>
                                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                </div>
                            <% } %>
                        <% } %>
                        <!-- Course Table -->
                        <div class="d-flex justify-content-between align-items-center mt-4">
                            <h2>Class</h2>
                            <a href="/i/course"  class="btn btn-secondary">Refresh</a>
                        </div>
                        <table class="table table-striped table-sm">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Class Code</th>
                                    <th>Subject</th>
                                    <th>Class Credit</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (courses || courses.length > 0 ) { %>
                                    <% courses.forEach((course, index) => { %>
                                        <tr>
                                            <td><%= index + 1 %></td>
                                            <td><%= course.course_code %></td>
                                            <td><%= course.course_name %></td>
                                            <td><%= course.course_credit %></td>
                                            <td>
                                                <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#courseModal<%= course.course_id %>">
                                                    View Sections for <%= course.course_name %>
                                                </button>
                                                <button type="button" class="btn btn-warning btn-sm" data-bs-toggle="modal" data-bs-target="#editModal<%= course.course_id %>">
                                                    Edit
                                                </button>
                                                <button type="button" class="btn btn-danger btn-sm" data-bs-toggle="modal" data-bs-target="#deleteModal<%= course.course_id %>">
                                                    Delete
                                                </button>
                                            </td>
                                        </tr>

                                        <!-- DELETE Modal -->
                                        <div class="modal fade" id="deleteModal<%= course.course_id %>" tabindex="-1" aria-labelledby="deleteModalLabel<%= course.id %>" aria-hidden="true">
                                            <div class="modal-dialog">
                                                <div class="modal-content">
                                                    <div class="modal-header">
                                                        <h5 class="modal-title" id="deleteModalLabel<%= course.id %>">Confirm Deletion</h5>
                                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                    </div>
                                                    <div class="modal-body">
                                                        Are you sure you want to delete the class "<%= course.course_name %>"?
                                                    </div>
                                                    <div class="modal-footer">
                                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                                        <!-- The actual delete action -->
                                                        <a href="/i/del/<%= course.course_id %>" class="btn btn-danger">Confirm Delete</a>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>

                                        <div class="modal fade" id="editModal<%= course.course_id %>" tabindex="-1" aria-labelledby="editModalLabel<%= course.course_id %>" aria-hidden="true">
                                            <div class="modal-dialog">
                                                <div class="modal-content">
                                                    <div class="modal-header">
                                                        <h5 class="modal-title" id="editModalLabel<%= course.course_id %>">Edit Class</h5>
                                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                    </div>
                                                    <div class="modal-body">
                                                        <!-- Edit form to update the course details -->
                                                        <form action="/i/edit/<%= course.course_id %>" method="post">
                                                            <div class="mb-3">
                                                                <label for="courseCode" class="form-label">Class Code</label>
                                                                <input type="text" class="form-control" id="courseCode<%= course.course_id %>" name="course_code" value="<%= course.course_code %>" required>
                                                            </div>
                                                            <div class="mb-3">
                                                                <label for="courseName" class="form-label">Class Name</label>
                                                                <input type="text" class="form-control" id="courseName<%= course.course_id %>" name="course_name" value="<%= course.course_name %>" required>
                                                            </div>
                                                            <div class="mb-3">
                                                                <label for="courseCredit" class="form-label">Class Credit</label>
                                                                <select class="form-control" id="courseCredit<%= course.course_id %>" name="course_credit" required>
                                                                    <option value="1" <%= course.course_credit === 1 ? 'selected' : '' %>>1</option>
                                                                    <option value="2" <%= course.course_credit === 2 ? 'selected' : '' %>>2</option>
                                                                    <option value="3" <%= course.course_credit === 3 ? 'selected' : '' %>>3</option>
                                                                </select>
                                                            </div>
                                                            <div class="modal-footer">
                                                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                                                <input type="submit" class="btn btn-primary" value="Save Changes">
                                                            </div>
                                                        </form>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>

                                        <!-- modal Part -->
                                        <div class="modal fade" id="courseModal<%= course.course_id %>" tabindex="-1" aria-labelledby="courseModalLabel<%= course.course_id %>" aria-hidden="true">
                                            <div class="modal-dialog">
                                                <div class="modal-content">
                                                    <div class="modal-header">
                                                        <h5 class="modal-title" id="courseModalLabel<%= course.course_id %>">Sections for <%= course.course_name %></h5>
                                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                    </div>
                                                    <div class="modal-body">
                                                        <ul class="list-group">
                                                            <% classes.filter(section => section.class_course === course.course_id).forEach(function(section) { %>
                                                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                                                    <div>
                                                                        <strong>Section:</strong> <%= section.class_section %> <br>
                                                                        <strong>Classroom:</strong> <%= section.class_room %> <br>
                                                                        <strong>Instructor:</strong> <%= section.class_ins %> <br>
                                                                        <strong>Password:</strong> <%= section.class_password %> <br>
                                                                    </div>
                                                                    <form action="/i/delete/section" method="post" onsubmit="return confirm('Are you sure you want to delete this section?');">
                                                                        <input type="hidden" name="section_id" value="<%= section.class_id %>">
                                                                        <input type="hidden" name="course_id" value="<%= section.class_course %>">
                                                                        <input type="submit" class="btn btn-danger btn-sm" value="Delete">
                                                                    </form>
                                                                </li>
                                                                <br>
                                                            <% }) %>
                                                        </ul>
                                                    </div>
                                                    <div class="modal-footer">
                                                        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addSectionModal<%= course.course_id %>">
                                                            Add Section
                                                        </button>
                                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                        <!-- Add Section Modal for Each Course -->
                                        <div class="modal fade" id="addSectionModal<%= course.course_id %>" tabindex="-1" aria-labelledby="addSectionModalLabel<%= course.course_id %>" aria-hidden="true">
                                            <div class="modal-dialog">
                                                <div class="modal-content">
                                                    <div class="modal-header">
                                                        <h5 class="modal-title" id="addSectionModalLabel<%= course.course_id %>">Add Section for <%= course.course_name %></h5>
                                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                    </div>
                                                    <div class="modal-body">
                                                        <!-- Add Section Form -->
                                                        <form action="/i/section" method="post">
                                                            <input type="hidden" name="course_id" value="<%= course.course_id %>">
                                                            <div class="mb-3">
                                                                <label for="section" class="form-label">Section</label>
                                                                <input type="text" class="form-control" id="section" name="section" required>
                                                            </div>
                                                            <div class="mb-3">
                                                                <label for="classroom" class="form-label">Classroom</label>
                                                                <input type="text" class="form-control" id="classroom" name="classroom" required>
                                                            </div>
                                                            <div class="mb-3">
                                                                <input type="hidden" class="form-control" id="class_ins" name="class_ins" value="<%= course.ins_id %>">
                                                            </div>
                                                            <div class="mb-3">
                                                                <label for="password" class="form-label">Password</label>
                                                                <input type="text" class="form-control" id="password" name="password" required>
                                                            </div>
                                                            <div class="modal-footer">
                                                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                                                <input type="submit" class="btn btn-primary" value="Save Section">
                                                            </div>
                                                        </form>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="5" class="text-center">No courses found</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </main>
                </div>
            </div>
        </div>
        <!-- Tab 2: Blank Page -->
        <div class="tab-pane fade" id="tab2" role="tabpanel" aria-labelledby="profile-tab">
            <div class="col-md-4">
                <a class="card  text-decoration-none" style="color:black;" href="#">
                    <div class="card-body">
                        <h5 class="card-title">Meetings</h5>
                    </div>
                </a>
            </div>
        </div>
        </div>
    </div>
</div>
<!-- Modal create course -->
<div class="modal fade" id="createCourseModal" tabindex="-1" aria-labelledby="createCourseModalLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="createCourseModalLabel">Create New Course</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <!-- Modal content, such as a form to create a course -->
                        <form action="/i/course" method="post">
                            <div class="mb-3">
                                <input type="hidden" class="form-control" name="id"  value="<%= id %>">
                            </div>
                            <div class="mb-3">
                                <label for="courseCode" class="form-label">Class Code</label>
                                <input type="text" class="form-control" name="courseCode"  placeholder="Enter course code" required>
                            </div>
                            <div class="mb-3">
                                <label for="courseName" class="form-label">Class Name</label>
                                <input type="text" class="form-control" name="courseName"  placeholder="Enter course name" required>
                            </div>
                            <div class="mb-3">
                                <label for="courseCredit" class="form-label">Class Credit</label>
                                <select class="form-control" name="courseCredit" >
                                    <option value="" disabled selected>Select class credit</option>
                                    <option value="1">1</option>
                                    <option value="2">2</option>
                                    <option value="3">3</option>
                                </select>
                            </div>
                            <hr>
                            <div class="mb-3 text-end">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                <input type="submit" class="btn btn-primary" value="Add course">
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>